# Task queue

| ID | Task | Phase | Status |
|---|---|---|---|
| T-0001 | Release 0.1 foundation | 1 | **done** — tagged `v0.1.0` |
| T-0002 | Keycloak realm + client + docker wiring | 2 | **done** — realm as configuration-as-code |
| T-0003 | Users, organizations, branches, memberships | 2 | **done 2026-07-27** — `BranchService`, `UserService`, `MembershipService` + controllers on the `OrganizationService` pattern. 8 routes live under `/api/v1`, all 401 unauthenticated. Role gates, role allow-list, audit, one-way withdrawal. **`viewerGrants()`/`viewerRole()` still demo — replacing them is T-0005 (session wiring), not more services** |
| T-0004 | Roles, permissions, permission matrix | 2 | **partial 2026-07-27** — `apps/api/src/authz/permission-matrix.ts` maps all 13 grantable roles to the 3 permission keys the nav gates on, from 07 pt2 §50 + 01 §29/§32. Fails closed on an unknown role. Deliberately small: new keys arrive with the modules that gate on them |
| T-0005 | Tenant context resolution from validated claims | 2 | **finding 5 CLOSED + BOTH GATES PASSED 2026-07-28** (`6725b14`) — sign-out now revokes for real, proven by an A/B refresh grant (200 without sign-out, 400 `invalid_grant` with). Codex found 2 MEDIUM, Supervisor found 3 MORE that Codex missed — incl. logout sending no `client_id`, which would have left the SSO session alive after any refresh that dropped the id token. **Finding 4 CLOSED 2026-07-28** (`9560c7a`) — gated at the layout (display, stops enumeration) AND at each concrete page via `requireWorkspaceAccess()` (the real one). A layout gate alone was PROBED and does NOT stop the page executing or its output reaching the RSC flight payload. `scripts/guardrails/check-page-gates.sh` makes the page gate a build gate, with a 9/9 self-test covering every bypass Codex named. Earlier: **code complete 2026-07-27** (`0b678b5`) — `packages/auth` (Auth.js v5 + Keycloak, one factory x7), session-backed `viewerGrants()`/`viewerRole()` via `GET /api/v1/me`, all 7 apps wired with async layouts + middleware + route handler. Codex and Supervisor have NOT reviewed. Playwright NOT re-run. Earlier API side: `KeycloakJwtService`, `TenantGuard`, and now **`GET /api/v1/me`** returning userId/tenant/org/branch/activeRole/permissions/memberships, all derived server-side. **REMAINING: the Next apps have no session at all** — no Auth.js, so `viewerGrants()`/`viewerRole()` are still demo data |
| T-0035 | **Phase 4 slice 1 — customers + vehicles** | 4 | **done 2026-07-28** — migration 004 (`core` schema, real FKs + joins + normalised makes/models, ENABLE+FORCE RLS, tenant index baseline) + 005 (uniqueness moved to organization scope, closing a cross-org existence oracle). `CustomerService`/`VehicleService` on the `OrganizationService` shape, 6 routes under `/api/v1`. Screens live at all THREE role-tree paths (§34 default, §46 owner, §48 reception) from one shared implementation in `app/_screens/`. **Proven signed in through real Keycloak in BOTH directions**: reception sees 3 Tenant A customers and never Tenant B's; a technician gets 404 on the page AND 403 from the API. Codex 2 findings + Supervisor 1, all fixed — `reviews/phase4-slice1-customers-vehicles.md` |
| T-0036 | **Phase 4 slice 2 — the WRITE path: register customer + register vehicle** | 4 | **done 2026-07-28** — `apiPost` (never throws; `invalid` carries the API's own message), server actions in a `'use server'` module, shared form controls. React is **18.3.1** so `useFormState` is unavailable — the client form calls the action directly, which also keeps inputs uncontrolled so nothing typed is lost on a rejected submit (`01 (1).txt` §3553). Vehicle form posts **foreign keys** from real pickers (`customerId`, `makeId`), not typed names — new `GET /vehicle-makes`. **Proven by actually submitting**: customer created, vehicle created, duplicate registration refused with *"a vehicle with this registration number or VIN already exists"*, and the new vehicle then appears in the list joined to its owner. Codex found 1 (invalid emails persisted — `noValidate` disabled the browser check and the server had none), fixed server-side + `checkValidity()` |
| T-0037 | **Phase 4 slice 3 — customer + vehicle DETAIL pages** | 4 | **done 2026-07-28** — 6 dynamic routes (one per role tree), list rows now link through. Customer detail shows the customer AND their vehicles via `GET /customers/:id/vehicles` — the relationship, navigable. Vehicle detail links back to its owner. **A detail route gates on its PARENT LIST route**, because no navigation advertises one entry per record; `check-page-gates.sh` strips trailing dynamic segments and enforces it, self-test now **23/23** including the shape that gates on its own literal `[id]` path and would always 404. Proven live: reception sees Kwame Mensah with both vehicles; a **Tenant B customer id typed directly returns "Not found"**; a technician gets nothing. Codex: no findings |
| T-0038 | **Phase 4 — the CUSTOMER-facing vehicle garage** (`01 (1).txt` §33) | 4 | **done 2026-07-28** — `/my-vehicles/garage` on customer-web. **This is the screen that finally exercises owner-scoping**: `01 (1).txt` §19 "vehicle owners shall see only vehicles they own" was written, unit-tested against a mock, and had NEVER run against real data because every seeded `core.customers.user_id` was NULL. Seed now links a `customer` dev identity, asserting exactly one match. **Proven: the customer sees 2 of the tenant's 4 vehicles — Adjoa's `GW 7745-21` is excluded even though it is the SAME tenant AND the same organization**, which RLS structurally cannot do. Same `GET /vehicles` endpoint as the workshop list; only the viewer differs. API probed directly: 2 vehicles, 1 customer. Codex found 2, both fixed (seed matched on a non-unique name; a comment overstated what the gate enforces — signed-out visitors get 200 + the unauthenticated state, verified) |
| T-0039 | **Add Vehicle on customer-web + shared form controls into `packages/ui`** | 4 | **done 2026-07-28** — the customer registers their OWN vehicle. **No owner picker**: `GET /customers` returns exactly one row for a `customer` viewer, so the id is resolved rather than chosen, and `VehicleService.create` re-checks it with `user_id = ctx.userId`. **Tamper-tested**: POSTing another customer's id with a real customer token → **404 `customer not found`**, no row. Full loop proven — customer adds it, it appears in their garage AND in the workshop's register attributed to Kwame Mensah. Form controls MOVED from workshop-web into `packages/ui` (Directive §3, no duplicate module). Codex found `engineType` was sent but had no field — true of the WORKSHOP form too; both fixed, persistence verified (`2KD-FTV`) |
| T-0016 | **Workspace / organisation / branch switchers** | 3 | **organisation switcher DONE 2026-07-28** — native `<select>` in the top bar (keyboard-accessible, works without JS, no focus-trap plumbing). Selection stored in a NON-httpOnly cookie and sent as `x-organization-id`, which `TenantGuard` already understood; the API re-validates it against the viewer's own memberships and REFUSES one they do not hold, so the cookie is a preference and never a credential. **Fixed a latent LOCKOUT in `resolveTenantContext`**: it threw when a user held >1 membership and had made no selection — including on `/me`, so they could never load the shell containing the switcher. Now a deterministic default. **Codex found a SECOND lockout of the same shape** (a STALE selection failed `/me` forever); `/me` now retries without the header and data calls drop a selection the viewer no longer holds. Proven live: Alpha Parts Supply (1 customer) → switch → Alpha Motors (3 customers), label and data moving together; and a planted stale cookie still renders the shell. **Branch + workspace switchers remain** |
| T-0040 | **Customer dashboard — the landing page reads real data** | 4 | **done 2026-07-28** — `/home/dashboard` on customer-web fell through to the catch-all placeholder, so the FIRST screen a customer saw told them nothing about their own vehicles. Now: their vehicles (own only), and an INSURANCE assessment computed from real dates — lapsed / due within 30 days / in date / **not assessed**, the last stated explicitly so an unchecked vehicle is never implied to be fine. Deliberately NO invented tiles (`05.txt` §2): service history and appointments are named as not built rather than mocked. Proven live: 2 own vehicles, `GR 4821-22` expired, `GT 1190-19` due in 15 days, Adjoa's vehicle absent |
| T-0041 | 🟢 **PHASE 5 STARTED — job cards** (`1.txt` §322, the repair lifecycle) | 5 | **slice 1 done 2026-07-28** — migration 006 `repair.job_cards` with the **19 spec stages + on_hold** transcribed from `1.txt` §322-§360 and `02.txt` §29, real FKs to `core`, race-safe **per-organization** job numbers (a global sequence would leak platform volume across tenants). `JobCardService` gives THREE scopes from ONE query: staff see the organisation, a **technician sees only cards assigned to them**, a customer only cards against their own vehicles. Screens at 3 role-tree routes; the technician's `My Assigned Work` is the SAME screen. Codex found 2, both fixed (assignee was only checked for membership, not the technician ROLE — a card assigned to a cashier appears on nobody's list; plus an unused import). 18 new unit tests |
| T-0042 | **Phase 4 leftover — customer complaint submission** (`2.txt` §537) | 4 | **done 2026-07-28** — `Report a Problem` on customer-web OPENS a job card at stage `complaint_received`, rather than creating a separate complaint someone must later convert: one record, one lifecycle, no handover to forget. **Proven end to end**: customer submits → `JC-000005` → the workshop's Job Cards screen shows it with the customer's own words. Text only, and the screen SAYS SO — §537's voice/photo/video need file storage this build does not have, and a disabled camera button would imply otherwise |
| T-0043 | 🟢 **Phase 5 slice 2 — stage transitions + Repair Staging Board** | 5 | **done 2026-07-29** — `PATCH /job-cards/:id/stage` + `GET /job-cards/board` + the board at both role-tree routes. `1.txt` §394 implemented as **two independent checks**: `ROLE_TARGET_STAGES` (07 pt2 §50 — who may produce a stage) and `STAGE_TRANSITIONS` (where a card may go), with an **authorized, logged override** that relaxes the second and never the first. Migration **008** `job_card_stage_events` (append-only — needs an explicit `REVOKE`, because 006's `ALTER DEFAULT PRIVILEGES` already granted UPDATE/DELETE) + **009** composite FK and stage CHECKs. **`on_hold` resumes to the stage the HISTORY records**, which is the whole reason the table exists: a static exit list would let anyone park a job on hold and resume it past quality control without tripping the override. **Proven with real tokens, not from the screen**: technician → `quality_control` 403, unassigned card 404 (not 403), resume→`testing` refused with the permitted list, override without a reason 400 / with one 200 and `is_override=t`, and an `UPDATE` of the reason denied. Codex 2 (both fixed), Supervisor 1 more Codex missed (`completed→warranty_follow_up` erased `closed_at`). Record: `reviews/phase5-slice2-stage-transitions.md` |
| T-0045 | 🔴 **The Playwright suite ran ZERO tests and exited 0** | 3 | **fixed 2026-07-29** — `shell-journey.spec.ts` imported `grantsFor`/`navRoleFor` through the `@autoworkshop/next-shell` BARREL, which evaluates `index.ts` → `ModulePage` → `viewer` → `@autoworkshop/auth` → `next-auth`, and next-auth cannot resolve `next/server` outside a Next runtime under pnpm's store. The suite died at COLLECTION and **Playwright still exited 0**, so `pnpm e2e` reported success while running nothing — undetected from `0b678b5` (2026-07-27, T-0005) because nothing re-ran it. `index.ts` already documents that the pure half exists "so consumers that cannot run in a Next server runtime — the Playwright journey — can reason about a viewer"; re-exporting it through the barrel does not deliver that. Now imported from `next-shell/src/viewer-contract` directly. **Three stale assertions then surfaced, all from the 07-27/07-28 auth work, none a product defect** — see T-0046. Suite now **138 passed / 2 skipped / 0 failed** |
| T-0046 | Three shell-journey assertions predated authentication | 3 | **fixed 2026-07-29** — (a) a gated URL on **admin** returns 200, not 404: the 07-28 layout gate stops a signed-out viewer before the catch-all can `notFound()`. **Verified it is not an oracle** — `/home/operations-dashboard`, `/home/total-nonsense-xyz` and `/directory/users` all return 200 and the body names the workspace ("Platform Admin") but **never the module** ("Operations Dashboard"). Assertion widened to 404-or-unnamed-200, so the original defect (the placeholder printing `item.label`) still fails it. (b) `admin advertises no modules` is CORRECT for a signed-out viewer since §32 gates the whole tree; skipped on the derived condition, not by naming admin. (c) T-0005's "Sign in" link inside `<nav>` counted as a stray; `/api/auth/*` now excluded exactly as `/` already was |
| T-0044 | Document scrolls 51px sideways at a **768px** viewport | 3 | **open — pre-existing, NOT from T-0043.** Measured 819 vs 768 on `/home/dashboard`, `/customers/customer-search` and `/workshop-floor/job-cards` as well as the new board. A shell defect at tablet width; 1280 and 390 are clean |
| T-0034 | ✅ **LIVE OUTAGE 2026-07-28 — CAUSE CONFIRMED: free-tier 750 instance-hours reached; reactivates 1 August** | 3 | **RESOLVED, no code action.** Owner confirmed from the dashboard. Re-run Release once it serves. Two always-on free services cannot share one 750h allowance — a month is ~730h. Earlier detail: Every request 503 in <0.5s (not a cold start). `POST /resume` → 400 *"only services suspended by a user can be resumed"* — a billing suspension cannot be lifted via the API. Staging was DELETED (owner approved) so production carries the free allowance alone; that stops future consumption but does not restore a consumed one, and production stayed 503. **Not caused by any 07-28 commit** — checks and image jobs pass on all of them. See `docs/13-operations/LIVE-OUTAGE-2026-07-28.md` |
| T-0032 | Production + staging deployment of `workshop-web` | 3 | **staging DELETED 2026-07-28**; was **done 2026-07-27** — `autoworkshop.aiappinvent.com` LIVE. Render's builder is out of the critical path: CI builds a container, starts it, smoke-tests `/api/auth/*`, publishes to GHCR; Render pulls a prebuilt image (ADR-017). Two free image services — staging `srv-d9jun8m417fc73dore50`, production `srv-d9ju49id0e5s7389fjlg`. Production is unreachable unless the identical digest-tagged image already serves on staging. `autoDeploy` off on both. The old node service `srv-d9jsliu7r5hc73b1kncg` never deployed successfully and is retired, domain detached. **The 448 MB heap theory was reproduced in CI and REFUTED; the underlying Render build fault is still unexplained** |
| T-0006 | RLS FORCE + tenant-isolation test suite | 2 | **partial** — RLS proven as non-superuser; full suite outstanding |
| T-0007 | Audit framework (append-only) | 2 | **done** — `AuditService`, same transaction as the work it records |
| T-0008 | WAL archiving + PITR + off-host backup + restore drill (Supervisor C3) | 2 | **done** — archiving fixed (had NEVER worked), drill passes 4/4, RTO 16-106s, RPO 0 |
| T-0009 | Top navigation bar | 3 | **done** |
| T-0010 | Collapsible grouped side navigation | 3 | **done** |
| T-0011 | Shell surfaces: tabs, dialogs, drawers, AI assistant panel | 3 | **done** |
| T-0012 | Runtime theming (light / dark / system) | 3 | **done** |
| T-0013 | Responsive shell — mobile overlay nav, tablet behaviour | 3 | **done** |
| T-0014 | Storybook stories for every shell component (`01 (1).txt` §71) | 3 | **done** — 10 story files, 77 stories, axe 84/84 |
| T-0015 | Playwright shell journey + axe-core accessibility gate | 3 | **done** — `apps/e2e`, 138 passing, harness defects fixed 2026-07-27 |
| T-0016 | Workspace / organisation / branch switchers | 3 | **unblocked on the data side** (T-0003 done); still needs T-0005 session wiring to know who the viewer is |
| T-0017 | Quick-create, tasks, messages, notifications, help panels (§9-§14) | 3 | queued |

| T-0018 | Schedule the backup + drill | 2 | **done** — 4 Windows tasks live + `autoworkshop-backup.cron` for production; all 4 proven by triggering, `LastResult 0x0` |
| T-0019 | Alert on backup age and on `pg_stat_archiver.failed_count` rising | 2 | **partial** — `check-backup-health.sh` detects and exits non-zero (live: HEALTHY 7/7); delivery is cron-mail only, nothing notifies on Windows |
| T-0020 | Drill a restore from the OFF-HOST copy alone | 2 | queued |
| T-0021 | MinIO object-lock / immutability (needs a bucket rebuild) | 2 | queued |
| T-0022 | Rebuild the local cluster with `--data-checksums` on | 2 | queued |

| T-0023 | Deliver the health-check alert somewhere a human sees it (closes T-0019) | 2 | queued |
| T-0025 | ~~axe `color-contrast`~~ | 3 | **withdrawn** — the 10 hits came from stories rendering Storybook's error page, not from the palette |
| T-0026 | Dangling `aria-controls` (nav toggle + every collapsed SideNav group) | 3 | **done** — both were real; axe rated them CRITICAL |
| T-0027 | Navigation model becomes **workspace x role** (07 pt2 §46-§50) | 3 | **done 2026-07-27** — 4 role trees (§46-§49) beside the §34 workspace default; `viewerRole()` is the single decision point for BOTH the shell and the catch-all router. Verified live: §49 routes 200, §34-only routes 404. **Phase 5 unblocked** |
| T-0028 | Account types as *requests*, workshop staff invitation, approval limits | 2 | queued |
| T-0029 | Plan extension v1 r2 — specs 07/08/09 folded into the phase plan | — | **done** — `docs/00-project/PLAN_EXTENSION_v1.md` |
| T-0030 | ~~Side nav renders INLINE at 360px~~ | 3 | **closed 2026-07-27 — NOT A PRODUCT DEFECT.** A stale `next start` server was serving chunk hashes a later rebuild had deleted; every chunk 404'd, React never hydrated, so `useIsMobile()` never left its SSR default. Reproduced under control (main 103px, overflow 161px, `__react*` absent) and fixed with a build-freshness gate |
| T-0031 | ~~ThemeToggle radiogroup: arrows move focus but not selection~~ | 3 | **closed 2026-07-27 — NOT A DEFECT.** Same stale-server cause as T-0030: with no hydration `setPreference` never ran, so `aria-checked` never changed. The roving tabindex and arrow handling were already correct (shipped in the defect-4 fix). Both tests pass on a fresh build |
| T-0024 | Review guardrails: RAG grounding, claim verification, scoped review, idiom lint | 2 | **done** — 4 layers in `scripts/guardrails/`, wired as Stage 0 of the quality gate |

**Next up:** T-0016 switchers (visible), then **PHASE 4 — Customer + Vehicle**, the owner's stated
priority and the first phase with real screens. Owner rule binding on Phase 4's schema: **real
foreign keys, joins, normalised** — relationships give integrity, RLS gives isolation, both required.

**T-0033 — three follow-ups from the 2026-07-28 Supervisor pass** (none blocking; production has
no deployed identity yet, so it renders the signed-out shell):
1. `AUTH_URL` is **absent from `render.yaml`**. Set it per service so neither the session-cookie
   name nor the post-logout origin rests on a request header.
2. **Confirm a realm/deployment origin mismatch:** `render.yaml` deploys `workshop-web` at
   `autoworkshop.aiappinvent.com`, but `autoworkshop-workshop-web`'s `redirectUris` are
   `http://localhost:3001/*` and `https://workshop.autoworkshop.aiappinvent.com/*`. If that is
   what is live, the deployed origin is not in its own client's allow-list — sign-in AND sign-out
   both fail the moment identity goes live.
3. **No audit event on logout**, which CLAUDE.md §9/§16 require. The one outcome that leaves a
   live credential behind currently exists in stdout only.

**A defect worth remembering, found by running the app rather than reviewing it:** the session
cookie NAME was chosen from `NODE_ENV` while Auth.js chooses it from the URL scheme. On the live
https site the two coincide, so it works; under `next start` over http a genuinely signed-in user
resolved to nobody — `/api/auth/session` named them while the server-side shell rendered "Not
signed in" and `viewerGrants()` returned none. No local production build had ever resolved a
viewer. Nothing logged an error.

**Superseded:** T-0005 — tenant context from the Keycloak session inside the Next apps. It is now the single blocker for T-0016 (switchers) and is what actually replaces the demo bodies of `viewerGrants()` and `viewerRole()`. Then T-0023, then T-0017.

**Phase 5 is unblocked.** T-0027 landed the workspace × role navigation it was waiting on.

**All four tests left red at the 2026-07-26 close were one environmental fault.** Three were T-0030 and one was T-0031; none was a defect in the shell. The cause was a `next start` server serving a build that had been deleted underneath it.

**The Phase 2 backup thread is now closed except for delivery.** T-0008 is done and drilled (RTO
16–106 s, RPO 0, 4/4 runs); T-0018 is done and every job has actually fired; T-0019 detects but does
not deliver, which is T-0023. Live health as of 2026-07-26T19:47Z: **HEALTHY 7/7**, WAL
`archived=50, failed=0`.

⚠️ **T-0018/T-0019 shipped without a review gate.** Commit `71a17fd` left no record in `reviews/`
and did not update this file — the two facts are related. Retro-reviewed 2026-07-26.

**The retro-review was not a formality — it found a CRITICAL and a HIGH sitting in the DR
subsystem:** the off-host-copy check reported `OK` when there were **zero** off-host backups
(`grep -c` prints `0` and exits 1, so `|| echo 0` made `N` the string `"0\n0"` and the integer test
fell through to the healthy branch — correct on the healthy path, wrong on the only day it matters),
and the lock was per-job, so daily 02:15 overrunning into weekly 03:15 gave two concurrent
`pg_basebackup`s that the file's own header promised were impossible. Both fixed and re-verified.
See `reviews/codex-review-t0018-scheduling.md` and
`reviews/supervisor-adjudication-t0018-scheduling.md`.
