import { registerRootComponent } from 'expo';
import App from './App';

// `registerRootComponent` calls `AppRegistry.registerComponent('main', ...)` and
// sets up the environment for both Expo Go and a native build, so the same entry
// point works in development and in a release APK.
registerRootComponent(App);
